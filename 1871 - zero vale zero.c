#include <stdio.h>
#include <stdlib.h>
#define MAX 999999999

int main () {

    double M[MAX], N[MAX], x;

    scanf("%lf %lf", &M, &N);

    for(i = 0; M >= 1; M++){
        for(j = 0; N >= 1; N++){
            x = M + N;
        }
    }

       


    return 0;
}